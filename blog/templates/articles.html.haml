%ul
  -for article in articles
    %li
      .title
        = article.title
      .slug
        = article.slug
      .preview
        = article.preview
      .author
        = article.author.username
