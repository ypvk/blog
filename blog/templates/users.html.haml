-extends "layout/base.html.haml"
- block body
  .container
    %a{'href': "{% url user_new %}"}
      = 'create a user'
    %ul
      -for u in users
        %li.user
          %span
            %a{'href': "{% url user u.username %}"}
              = u.username
          %span= u.email
